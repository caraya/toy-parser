# TODO

- [ ] **Adoption Agency Algorithm**: Implement the algorithm for handling misnested formatting elements (e.g., `<b><p>text</b></p>`). This is required for full HTML5 compliance in the "In Body" insertion mode.
- [ ] **Character References**: Implement full support for named (e.g., `&nbsp;`, `&amp;`) and numeric (e.g., `&#160;`, `&#x00A0;`) character references in the Tokenizer.
